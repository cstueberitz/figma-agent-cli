# Claude Profile

Purpose:
- Use Claude as a thin launch layer on top of the shared Figma backend.

How to Use:
- Read the repo root `AGENTS.md` before issuing Figma commands.
- Use `node src/index.js connect --safe` when plugin-based access is preferred.

Practical Guidance:
- Keep user-facing responses concise and action-oriented.
- Keep generated markdown and helper notes aligned with the shared repo structure instead of inventing Claude-only layouts.
